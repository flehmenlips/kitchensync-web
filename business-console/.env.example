# KitchenSync Business Console - Environment Variables
# Copy to .env and fill in your values. For Vercel, add these in Project Settings > Environment Variables.
#
# Vite only exposes variables prefixed with VITE_ to the client (via import.meta.env).
# All client-side config must use the VITE_ prefix.

# Supabase (required for auth)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Backend API (Render deployment)
VITE_BACKEND_URL=https://kitchensync-backend-2h5n.onrender.com
